package homework7;

/** Интерфейс Position с методом printPosition.
Этот интерфейс определяет контракт: любой класс, который его реализует, должен иметь этот метод.
 */
 public interface Position {
     void printPosition(); //Метод для печати названия должности. Без тела — это абстрактный метод.
}
